{
  "name": "insightflow",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/alert",
    "services/lead"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "dev:web": "npm run dev -w @insightflow/web",
    "dev:api": "npm run dev -w @insightflow/api",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1"
  }
}
